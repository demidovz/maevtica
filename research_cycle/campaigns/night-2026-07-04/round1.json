{"round":1,"source":"backlog","tested":[{"name":"Gated Feature (direction x context contract)","concept":{"name":"Gated Feature (direction x context contract)","definition":"A feature is a pair (v, g): direction v plus a sparse gate g — a predicate of <= 5 literals over co-occurring SAE-latent activity ('latent j active / inactive') — such that the mediation effect of patching along v conditional on g(x)=1 exceeds threshold theta, with g fit by decision tree / lasso on conditional-patching outcomes and validated on held-out inputs (precision >= 0.7). The four senses of 'feature' are predicted to coincide inside the gate and dissociate outside it; an ungated direction is not a feature, it is a marginal average over contexts.","prediction":"For >= 25% of SAE latents whose unconditional mediation effect falls in the bottom quintile of their layer, there exists a learnable gate (<= 5 literals, held-out precision >= 0.7) under which their mediation effect exceeds the layer median by >= 3x — i.e., most 'causally dead but interpretable' latents are context-marginalization artifacts. Directly computable with conditional activation patching. Falsified if conditional patching rescues < 5% of dead latents.","reduces_to":"Circuits / feature-interaction analysis (input-conditional feature effects in attribution graphs) — possibly just renaming 'a feature only matters inside its circuit' with a fitting procedure attached."},"test":{"design":"Preregistered in research_cycle/experiments/gated_feature/PREREG.md BEFORE running (thresholds fixed, committed to file). Model: gpt2 CPU, layer 7 (blocks.7.hook_resid_pre) with jbloom GPT2-Small SAE. Corpus: 120 fixed short prompts; context = prompt; BOS excluded. Sampled M=134 latents firing in >=25 prompts (fallback rule triggered as prereg allowed; reported). ONE intervention: per (latent, prompt) decoder-row ablation (subtract a_i*W_dec[i] at all firing positions); effect = max over post-first-fire positions of next-token KL(clean||ablated) [Amendment 1: run 1 read KL only at the last position and tripped the oracle; measurement fixed, decision rule untouched]. Dead set = bottom quintile of unconditional effect U_i. Gate family: conjunctions of <=5 literals over OTHER latents' prompt-level firing indicators (own activation excluded — trivial rescue banned); greedy forward selection on 60% train split. Rescue (held-out 40%): gate fires on >=3 test contexts, test precision >=0.7 for label [effect >= 3x layer median], and mean gated test effect >= 3x layer median. DECISION RULE (fixed pre-run): SUPPORTED if rescue_rate >= 0.25; REFUTED if < 0.05; else INCONCLUSIVE; shuffle control (effects permuted within latent, seed 2) demotes fake positives to INCONCLUSIVE_OVERFIT. Note: run artifacts existed from an earlier pass of this stage tonight; I verified the code implements the prereg line-by-line, then independently re-ran the measurement (diag.py) and reproduced every summary number exactly before accepting the verdict.","numbers":"M=134 latents, 27 dead (bottom quintile). layer_median=0.000808 nats, 3x threshold=0.002424 nats. rescued = 0/27 -> rescue_rate = 0.0 (< 0.05 refutation bound; prediction needed >= 0.25). Shuffle control rescue = 0/27. Decisive diagnostic (independent re-run, exact reproduction of all run-2 numbers): only 4/27 dead latents have ANY context with effect >= 3x layer median, 0/27 have >=3 such contexts, 5 high-effect (latent,context) pairs total across all dead latents — the rescue criterion is unsatisfiable even by an oracle gate, so the failure is not the gate learner. Dead latents are quiet in essentially every context, not marginalization artifacts. Files: research_cycle/experiments/gated_feature/{PREREG.md, exp.py, exp2.py, diag.py, result.json, diag.json, eff_run2.npz} in /home/friemann/workspace/repos-demidovz/maevtica/.","sanity":"Oracle passed AND earned its keep: run 1 FAILED the oracle (oracle_topq_median 0.0016 < 0.005 — last-position-only KL attenuated mid-prompt effects) and was correctly reported as BROKEN_MEASUREMENT, not a verdict. After the measurement fix, run 2 oracle: max U_i = 0.368 nats (huge, real ablation effect), top-quintile median U_i = 0.00608 >= 0.005 threshold; noise floor (zero-delta hook) = 0.0 < 1e-6. Independent re-run reproduced oracle_max 0.36802 and oracle_topq_median 0.00608 exactly. The pipeline demonstrably detects real effects; the dead latents' zeros are measured zeros.","status":"ran","verdict":"refuted"}}],"backlogLeft":[]}