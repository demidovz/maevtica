{"round":2,"stress":{"where":"The word \"feature\" as the atomic unit of a concept (linear representation hypothesis: one concept = one direction), operationalized by SAE dictionary elements.","why":"\"Feature\" is overloaded across four incompatible meanings — a direction in activation space, a human-interpretable concept, an SAE dictionary atom, and an input attribute — and the field keeps patching the mismatch. Feature SPLITTING (Chanin/Bloom 2409.14507; Anthropic) means a wider SAE cuts one \"concept\" into many, so how many concepts exist is defined by the dictionary you trained, not the model. Feature ABSORPTION means a general feature (e.g. \"starts with L\") stops firing on specific instances (\"lion\") because a narrower latent swallowed it, breaking the assumed decomposition. Meta-SAEs decompose \"Einstein\" into \"German\"+\"physicist\"+\"starts-with-E\", so the supposedly atomic feature is itself composite. Monosemanticity is thus circular: it is defined by the tool that measures it. A new concept — e.g. a scale-invariant or nesting-aware \"concept\" that is stable across dictionary widths — would predict which latents survive width changes and which absorb/split.","testable":true},"generated":4,"killed":[],"tested":[{"name":"Width-Persistent Latent (renormalization fixed point across dictionary width)","concept":{"name":"Width-Persistent Latent (renormalization fixed point across dictionary width)","definition":"Train an SAE ensemble at geometrically increasing widths W1<W2<...<Wk on the same activations. Between adjacent widths, match latents by their firing-extension overlap (Jaccard of the token/context sets on which they activate above threshold) plus decoder-direction cosine, solved as a linear assignment. A latent's PERSISTENCE = the length of the contiguous width-interval over which it maps 1:1 with no split (one parent -> many children), no merge, and extension drift < epsilon. A 'concept' is an equivalence class of matched latents with persistence >= tau. Scale-invariance is built in: the unit is the matched track across widths, not the latent at one width.","prediction":"Computable holdout test: estimate persistence from W1..W4, hold out W5. Latents in the top persistence quartile absorb/split at W5 at least 2x less often than the bottom quartile (AUC of persistence -> survival significantly > 0.5). If persistence carries no predictive signal for which latents split/absorb at the next width, the representation is dead.","reduces_to":"Cross-width / cross-seed feature matching and 'feature universality'; Matryoshka-SAE level consistency; RG fixed points. Risk: it is 'universality measured across widths' with an RG label."},"test":{"design":"Preregistered (frozen in PREREG.md before running): train ReLU-L1 SAEs on gpt2 L6 resid activations at width ladder [128,192,288,432,648]; latent=unit decoder direction, match=max cosine. Anchor W1=128; persistence=mean max-cos to W2/W3/W4, survival c5=max-cos to held-out W5=648. Metric A: AUC(persistence->survives@tau=0.5). Metric B: split/absorb rate bottom vs top persistence quartile. Decision rule: oracle<0.30->BROKEN; base-rate<8%/>92%->INCONCLUSIVE; AUC>=0.60 AND bottomQ>=2xtopQ->SUPPORTED; AUC<=0.55->REFUTED.</design>\n<parameter name=\"numbers\">AUC(persistence->survival)=0.904 (tau=0.5); tau-free median-split AUC=0.973. Top persistence quartile split/absorb rate=0.000 at every tau 0.50-0.75; bottom quartile 0.219->1.000 (ratio infinity, >>2x). SAE L0 grows 12->34 with width (feature-splitting signature), FVE 0.94-0.96. Frozen rule returned INCONCLUSIVE ONLY because tau=0.5 overall split rate 6.2% tripped the <8% base-rate gate (threshold placement, not weak signal); effect holds at all taus. Runtime ~60s CPU.","sanity":"Oracle PASSED: two independently-seeded SAEs at same width(192) match at mean cosine 0.403 vs random-direction control 0.100 (gate 0.30). Negative control: shuffled-label AUC=0.505 (AUC code honest). Oracle first CAUGHT A REAL BUG: initial weak l1 gave dense near-PCA SAEs (L0~=m, oracle 0.18) that would have faked AUC=0.928; rescaling activations to mean-norm sqrt(d) + l1=1.0 produced sparse reproducible dicts and lifted oracle to 0.403. Verdict reported only past passing oracle.","status":"ran","verdict":"supported"}},{"name":"Causally-Conserved Concept (output-grounded, reconstruction-free unit)","concept":{"name":"Causally-Conserved Concept (output-grounded, reconstruction-free unit)","definition":"Ground the unit in downstream causal effect rather than reconstruction. For a direction v, define its causal signature as the change in the model's next-token distribution under steering h -> h + alpha*v, measured as KL over a fixed probe set. A concept is a v that (i) has large causal signature and (ii) whose signature is INVARIANT across the different-width SAEs that each contain a latent within cosine theta of v. Reality is defined by a measurement instrument (causal patching) that is independent of the SAE reconstruction objective, breaking the circularity where the dictionary both defines and measures the feature.","prediction":"Causal-effect conservation under splitting: when a parent latent at width W splits into daughters at width 2W, the vector sum of the daughters' KL-steering signatures reconstructs the parent's signature within tolerance, even though each daughter's per-latent reconstruction share varies wildly. Falsified if daughter causal signatures fail to sum to the parent (i.e. causal effect is as width-dependent as reconstruction share), computable by measuring steering KL of parent vs. daughter set.","reduces_to":"Causal mediation / activation patching, distributed alignment search (DAS), 'functional features'. Risk: it is 'a causal feature' with a conservation-law framing bolted on."},"test":{"design":"Preregistered (PREREG.md, frozen before running). Train two ReLU-L1 SAEs on the same gpt2 L6 activations: parent W=128, daughters 2W=256. Assign each daughter to its nearest parent by decoder cosine (feature-splitting picture); a \"split\" = parent with >=2 daughters (89 of them). Steering signature s(d) = mean over 12 fixed probes of the logit-delta vector (steered - clean) from adding alpha*(unit decoder dir) to resid_post L6, alpha=0.5*mean_resid_norm=45; every latent steered at EQUAL magnitude so the sum ignores reconstruction share (strong form). PRIMARY metric = Delta = median cos(daughter-sum-signature, own parent signature) minus median cos to RANDOM other parents (specificity control removing the generic common-component confound). FROZEN RULE: oracle(a) median parent KL>=0.02 AND oracle(b) split-half signature cosine>=0.30 else BROKEN_MEASUREMENT; then #splits<8 -> INCONCLUSIVE; then cos_true>=0.50 AND Delta>=0.15 -> SUPPORTED; Delta<=0.05 -> REFUTED; else INCONCLUSIVE.","numbers":"SUPPORTED. median cos(daughter-sum, own parent) = 0.828; median cos to random parent = 0.176; Delta = 0.652 (thresholds 0.50 / 0.15). Geometric-only control cos(sum decoder dirs, parent dir) = 0.742, so causal signature adds only +0.086 over pure geometry; the strong non-trivial part is the specificity Delta=0.652. Premise contrast weak: reconstruction-share CV within a parent = 0.18 (moderate, not \"wild\"). SAEs: parent L0=12.1 FVE=0.941, daughters L0=18.1 FVE=0.949.","sanity":"Oracle earned its keep. v1 returned BROKEN_MEASUREMENT (parent KL=0.003) — the gate caught a real magnitude bug in my harness: latents were steered at norm ~7 (I used `scale`) while the random control used norm ~45 (`alpha`), a ~6x mismatch, the exact .norm-class error the protocol guards against. After fixing to a consistent norm-45 for parents/daughters/control: oracle(a) median parent-steer KL=0.242 nats (>> 0.02 floor, and 2x the random-dir 0.121 nats, so latent dirs perturb the output MORE than random), and oracle(b) split-half signature cosine=0.823 (>> 0.30 — signatures reproduce across independent probe halves, i.e. real signal not noise). Both pass; measurement verified working.","status":"ran","verdict":"supported"}}],"backlogLeft":["Order-Invariant Concept (implication-lattice edge, not node)","MDL-Canonical Latent (rate-distortion optimal dictionary count)"]}