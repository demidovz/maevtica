# Очередь опытов (готовы к запуску)

Каждый файл — готовый `args` для `cycle.workflow.js` (Workflow tool, scriptPath).
Запуск: `Workflow(scriptPath=..../cycle.workflow.js, args=<содержимое файла>)`.

| Файл | Что проверяет | Железо |
|---|---|---|
| ~~`separability_boundary.json`~~ | ❌ ЗАКРЫТА (sb3, 2026-07-06): на честном замере 410m наш датчик НЕ бьёт Park (0.21<порог 0.41, не тот знак; Park 0.57). sb1-лид был флуктуацией n=32. | — |
| ✅ `reflection_routing.json` | ПОДТВЕРЖДЕНО (rr4, 2026-07-06): нутро находит ошибки лучше слуха (AUC 0.84 vs 0.50, CI не вкл. 0, контроли чисты). Первый кирпич петли рефлексии. Оговорка: 1 модель/1 задача, детекция а не онлайн-использование. | CPU |
| `self_distill_lift.json` | ~~дистилляция в веса~~ — снята как исследовательская цель (переоткрытие STaR); держать только как навык | GPU |

Текущий фокус (по решению босса 2026-07-04): **понимание, не training-трюки.**
`separability_boundary` — прямое продолжение «съёмной категории», но нацелен на
слабость поля (граница провала separability), а не на его сильную сторону.
Порядок: ~~separability_boundary~~ (закрыта) → **`reflection_routing`** (следующая).

> ⚠️ **Лимит args (2026-07-04):** Workflow-транспорт режет большие args (~>2КБ)
> → мгновенный обвал `JSON Parse error: Expected ']'` (обрезка строки). Держи
> концепт в args КОМПАКТНЫМ (сути хватает), полный текст/prior-art — в этом
> файле как справка. sep-boundary стартовала с ужатой версии.
